{
  "variables": {
    "aws_access_key": "",
    "aws_secret_key": ""
  },
  "builders": [
    {
      "type": "amazon-ebs",
      "access_key": "{{user `aws_access_key`}}",
      "secret_key": "{{user `aws_secret_key`}}",
      "region": "us-east-1",
      "source_ami": "ami-0709aa3be6af7bd5c",
      "instance_type": "t2.micro",
      "ssh_username": "centos",
      "ami_name": "packer_september {{timestamp}}"
    }
  ],
  "provisioners": [
      {
          "type": "shell",
          "script": "/tmp/my_script.sh"
      }
  ]
}
